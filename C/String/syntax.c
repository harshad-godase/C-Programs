#include<stdio.h>
void main(){

    char name[] = "Harshad";
    puts("Your name is:");
    puts(name);

    char str[10];
    puts("Enter the string");
    gets(str);
    puts("you entered string is:");
    puts(str);

}